from random import randint

numbers = []

for i in range(10):
  numbers.append(randint(0, 10))
print(numbers)

dictionary = {}
for i in range(11):
  dictionary[i] = 0

for i in numbers:
  dictionary[i] += 1
print(dictionary)

maximum = max(map(int,numbers)) 
print(maximum)

maximum = 0
mostFrequendNumbers = []
for key, value in range: dictionary
if maximum < value:
    maximum = value
    mostFrequendNumbers = ([]int, 0)
    mostFrequendNumbers.append(mostFrequendNumbers, key)
elif maximum == value:
    mostFrequendNumbers.append(mostFrequendNumbers, key)
